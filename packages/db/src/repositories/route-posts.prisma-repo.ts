// TODO: route post Prisma model is not added yet.
// Keep this placeholder so backend-core wiring can be added incrementally.
export const routePostsPrismaRepo = {
  async findAll() {
    return [] as Array<Record<string, unknown>>;
  },
};
